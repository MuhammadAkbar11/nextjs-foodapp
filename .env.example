# Environment Configuration Template
#
# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored and MUST NOT be committed.
# This `.env.example` file is committed as a template and MUST contain no secrets.

# ---------------------------------------------------------------------------
# Public / runtime configuration (safe to expose to the browser)
# ---------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Future configuration (documented here for reference; not yet active)
# Add the corresponding real values to `.env.local` when the features land.
# ---------------------------------------------------------------------------

# Database connection (Issue 006 — Create Meals Database Schema)
# DATABASE_URL=postgresql://user:password@localhost:5432/foodapp

# Authentication secret (future auth work)
# AUTH_SECRET=
