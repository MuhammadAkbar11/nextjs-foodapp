/**
 * Database schema exports.
 *
 * Re-exports all Drizzle ORM table definitions so the rest of the
 * application can import them from a single location.
 */

export { meals, type MealRow, type MealInsert } from "./meals";

