/**
 * Database schema exports.
 *
 * This module is a placeholder for future Drizzle ORM table definitions.
 * Table schemas (e.g. meals, users) will be added by later issues and then
 * re-exported here so the rest of the application can import them from a
 * single location.
 *
 */

export {};
